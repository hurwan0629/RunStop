# RunStop 코스 생성 알고리즘 — 참고 자료

> AI 랭킹 모델(추천 모델 비교, RankNet 등)은 제외하고, 현재 코드 기준으로 검증된 알고리즘 부분만 정리했습니다.
> 슬라이드 형태가 아니라 발표 준비·Q&A 대비용 참고 문서입니다.
> 근거: `routing-worker/src/algo/*` 소스 코드, `docs/06_router-worker_명세/*`, `docs/06_router-worker_명세/05. 업데이트 기록/20260914_가중치_튜닝_근거_정리.md`

---

## 1. 전체 파이프라인

```
recommend() 요청
   ↓
① 후보 경로 생성        Weighted Dijkstra (개인화된 edge cost)
   ↓
② 후보 필터링           거리·겹침 하드컷 + 중복 제거
   ↓
③ Feature 추출          경사 · 시설 · 자연 · 노면
   ↓
④ 점수화                Raw Feature → Sub-score → condition_score
   ↓
⑤ AI 후보 선택          (현재 fallback: condition_score 정렬 — 이 문서에서는 다루지 않음)
   ↓
Top-K 추천 (기본 3개)
```

사용자가 설정한 `weights`(취향)와 `requirements`(필수 조건)는 ①~④ 전 단계에 걸쳐 전달됩니다.

---

## 2. 후보 경로 생성 — 개인화된 경로 탐색

**소스**: `routing-worker/src/algo/scoring/edge_cost.py`, `config.py`

단순 최단거리가 아니라, 도로 구간마다 사용자 취향을 반영한 비용을 계산해 Dijkstra로 탐색합니다.

```
Cost(도로) = 실제 길이 × 개인화 배율

개인화 배율 =
  거리 우선순위 (0.5~1.5)
  + 경사 가중치 × 경사 페널티
  + 안전 가중치 × 조명·CCTV 페널티
  + 자연 가중치 × 공원·하천 페널티
  + 노면 가중치 × 계단·신호 페널티
```

- 사용자 입력(0~5)을 `normalize_preference_weight()`가 0~1로 정규화해 가중치로 사용
- **다양성 확보**: 이미 다른 후보가 지나간 도로에는 reuse 페널티를 추가해, 최종 후보들이 서로 겹치지 않도록 유도
- **하드 제약**: 계단 회피, 최대 경사 같은 조건은 점수 감점이 아니라 탐색 단계에서부터 아예 배제

### EDGE_COST_SCALE_* 현재 값 (`config.py`, 2026-09-14 Valhalla 참고자료 검증)

| 상수 | 값 | 상태 |
|---|---|---|
| `EDGE_COST_SCALE_ELEVATION` | 1.2 | 유지 (러닝 특성상 최우선) |
| `EDGE_COST_SCALE_SAFETY` | 0.8 | 유지 |
| `EDGE_COST_SCALE_NATURE` | 0.6 | 유지 |
| `EDGE_COST_SCALE_SURFACE` | **1.0** | **0.8→1.0 상향** — Valhalla alley/driveway_factor가 문서화된 회피 계수 중 가장 공격적이라는 근거로 elevation과 동급으로 조정 |
| `EDGE_COST_SCALE_FLOW` | 0.5 | 유지 |
| `EDGE_SLOPE_GOOD_PCT` / `EDGE_SLOPE_BAD_PCT` | 2.0 / 10.0 | 유지 (Valhalla grade bucket과 일치) |

---

## 3. 후보 필터링

**소스**: `routing-worker/src/algo/routing/candidates.py`, `config.py`

| 기준 | 값 | 상수 |
|---|---|---|
| 거리 오차 허용 | 10% | `CAND_DIST_TOL_PCT` |
| 최대 겹침 허용 | 0.35 | `CAND_MAX_OVERLAP` |
| 중복 판정 기준(edge 공유율) | 0.6 | `DEDUP_SIMILARITY` |

- 두 후보의 edge 집합 교집합 비율이 `DEDUP_SIMILARITY`(0.6)를 넘으면 같은 코스로 간주해 뒤 후보 제거
- `out_and_back`(왕복)은 구조상 같은 길을 되돌아오므로 겹침 하드컷에서 예외 처리
- 방향(bearing)은 최대 `n_directions=12`개까지 시도

> **코드 확인 결과(중요)**: `generate_candidates`/`generate_candidates_via`에는 `pool=8`로 한 번 더 자르는 파라미터가 남아 있지만, 실제 슬라이싱(`[:pool]`)은 주석 처리되어 있어 **적용되지 않습니다.** 세 필터를 통과한 후보는 개수 제한 없이 전부 다음 단계(Feature 추출)로 넘어가고, 최종 개수는 점수화 이후 `top_k`(3)에서 결정됩니다. 문서(`14. Candidate Pool 필터링.md`)는 아직 "최대 8개"로 남아 있어 코드와 불일치 — 발표 때 "pool 상한"을 언급하지 않는 게 안전합니다.

---

## 4. Feature 추출 — 4가지 축

**소스**: `routing-worker/src/algo/features/*`

| Feature | 데이터 | 계산 방식 |
|---|---|---|
| 경사 (Slope) | DEM | 경로를 30m 간격 샘플링 → 구간 경사 계산 → 평균·최대·표준편차, 상승·하강고도 산출 |
| 시설 (Facilities) | 화장실·편의점 통합 CSV | 최근접 거리 + km당 개수(밀도) 산출 |
| 자연 (Nature) | 공원·하천 GeoJSON | 경로 인접 구간 비율(park_ratio, water_ratio) 계산 |
| 노면 (Surface) | OSM 도로 속성 (그래프 자체) | 보행 가능 비율, 계단 개수, 신호·횡단보도 밀도 계산 |

- 경사·시설·자연은 좌표(`coords`) 기반, 노면만 그래프의 도로 속성(`nodes+G`)을 직접 읽어 계산
- `slope_std_pct`는 절대 고도의 표준편차가 아니라 **구간별 경사 값의 표준편차** — 러닝 체감 난이도(오르락내리락)에 더 직접적인 지표

---

## 5. 점수 기준 튜닝 및 검증

**소스**: `routing-worker/src/algo/scoring/weighting.py`, 업데이트 기록(2026-09-14)

### 5-1. 방법론 (2단계)

| 단계 | 표본 | 조건 |
|---|---|---|
| 1차 | 출발점 40개 · 후보 120개 | `loop` 모드, 3km 단일 조건 |
| 2차 | 후보 507개 | `loop`/`point_to_point`/`out_and_back` 3모드 × 3km/5km/10km 3거리 × 출발점 12개 |



### 5-2. Weighting 상수 — 현재 코드 값 (검증 완료분)

| 상수 | 기존 → 신규 | 근거 |
|---|---|---|
| `DIST_ERR_ZERO_PCT` | 10% (유지) | 하드컷과 값 일치 필요 |
| `GAIN_PER_KM_ZERO` | 30m/km (유지) | 2차 실측 재확인 (p75~p90) |
| `WATER_RATIO_FULL` | 0.15 → **0.05** | 관측 최댓값(0.081)보다 높아 도달 불가능했음 |
| `WALKABLE_FULL` | 0.60 → **0.90** | p25(0.79)보다 낮아 상시 만점이었음 |
| `SIGNAL_PER_KM_ZERO` | 4.0 → **1.0** | 관측 최댓값(3.24)보다 높아 상시 고득점이었음 |
| `PARK_RATIO_FULL` | 0.15 (유지) | p90=0.21로 이미 합리적 위치 |
| `STREETLIGHT_PER_KM_FULL` | 120.0 (유지) | p90~max 사이로 이미 합리적 |
| `CCTV_PER_KM_FULL` | 15.0 (유지) | p75 근접, 이미 합리적 |
| `BIGROAD_ZERO` | 0.30 (유지) | p75~p90 사이로 이미 합리적 |
| `STAIRS_PENALTY` | 15 (유지) | 개수당 고정 감점 구조가 Valhalla step_penalty 철학과 일치 |
| `NEAR_OK_M` | 100m (→30m 설계) | 시설 집계 버퍼(100m)와 값이 같아 감점이 사실상 발동 안 함 (2차 실측 발견, 코드 미반영) |

### 5-3. 회귀 확인

- 신규 상수 적용 후 서울 실그래프에서 LOOP/ONE_WAY/ROUND_TRIP/경유지 4개 데모 케이스 재실행 → 전부 정상 동작
- `check_requirements`(화장실 최소 1곳 등) 하드컷은 변경 전후 동일 — 회귀 없음

### 5-4. 전제

> 이 상수들은 **실사용 로그가 없는 상태의 잠정치**입니다. 최종 목표는 사용자의 실제 선택·완주 로그를 Pairwise Learning to Rank로 학습해 이 손튜닝 점수 체계 자체를 대체하는 것이며, 이번 조정은 그 전까지 "죽은 지표"를 최소화하기 위한 중간 단계입니다.

---

## 발표/Q&A용 핵심 문장 모음

- "취향은 경로를 **찾는** 단계(edge cost)와 **평가하는** 단계(condition_score) 두 곳 모두에 반영됩니다."
- "후보 필터링에는 정해진 개수 상한이 없습니다 — 방향 최대 12개 중 필터를 통과한 만큼이 다음 단계로 넘어가고, 최종 3개는 점수화 이후 결정됩니다."
- "지금 상수들은 완벽한 최적값이 아니라, 507개 표본 기준으로 '죽은 지표'를 없앤 중간 단계이며, 실사용 데이터가 쌓이면 Pairwise LTR로 이 체계 자체를 대체할 계획입니다."
