# 환경변수 규칙

## 관리 위치

- 환경변수는 `src/config/env.ts`에서 검증 후 `env` 객체로 export합니다.

## 현재 주요 값

- `NODE_ENV`
- `PORT`
- `JWT_SECRET`
- `BCRYPT_SALT_ROUNDS`
- `SMS_API_ENABLED`
- `DATABASE_URL`
- `WORKER_URL`

## 기준

- 코드에서 `process.env`를 직접 읽지 않습니다.
- 새 환경변수는 `env.ts`와 `.env.example` 계열 문서에 함께 추가합니다.
