# 전체 조립 순서

## 관리 범위

- `app.ts` 미들웨어 등록 순서
- 라우터 등록 순서
- 404 처리와 전역 에러 처리 순서
- 서버 시작 파일과 앱 생성 파일의 책임 분리

## 현재 기준

1. 보안/파싱 미들웨어를 먼저 등록합니다.
2. 도메인 라우터를 등록합니다.
3. 매칭되지 않은 요청은 `notFoundHandler`로 넘깁니다.
4. 마지막에 `errorHandler`가 모든 에러 응답을 통일합니다.
