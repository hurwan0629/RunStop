# 요청 검증 규칙

## 기본 원칙

- Controller에서 Zod schema로 `req.body`, `req.params`, `req.query`를 검증합니다.
- 검증 실패 시 `ApiError`를 던집니다.
- Service에는 검증이 끝난 DTO만 넘깁니다.

## 에러 코드 기준

- 요청 형식이 틀린 경우 `INVALID_*_REQUEST`를 사용합니다.
- 인증 정보가 없거나 만료된 경우 인증인가 규칙 문서를 따릅니다.
- 도메인 조건이 맞지 않는 경우 Service에서 도메인 에러를 던집니다.
