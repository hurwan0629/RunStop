# DB 및 트랜잭션 규칙

## Pool

- DB 연결은 `infra/db/pool.ts`에서 관리합니다.

## Transaction

- 여러 테이블 변경이 하나의 흐름으로 묶이면 `withTransaction`을 사용합니다.
- Service에서 트랜잭션 범위를 결정합니다.
- Repository는 선택적으로 `PoolClient`를 받아 같은 트랜잭션 안에서 실행될 수 있어야 합니다.
