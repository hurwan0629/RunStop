`2026-08-28 15:44:11` : DB 스키마와 프로세스별 권한

현재 기준 DB는 도커 `docker-entrypoint-initdb.d` 대신 마이그레이션 + `CSV`/`SHP` 파일 `import` 방식을 사용하려 하고, 스키마를 `service`와 `datasets`를 나누어 큰 범위에서 역할을 나눌 생각입니다. 또한 Node와 Python에게 각각 WRITE 역할을 따로 주어 `Node-service`, `Python Worker: datasets`에 쓰기 권한을 줄 생각인데 규모에 비해 기술비용이 과하지 않은지 고민이 됩니다.

`2026-08-28 17:36:09`: 경사도 ui

사용자에게 경사도를 선택할 수 있게 할 때 `최종 상승 고도`, `경사의 편차` 등을 고려해야 한다고 생각하여 이걸 ai 추천 학습 대상으로 설정할지 ui를 통해 사용자가 더 확실하게 선택 가능하게 할지를 고민 중입니다.