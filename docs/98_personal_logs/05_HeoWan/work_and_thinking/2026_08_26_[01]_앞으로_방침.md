# [`2026-08-26 14:29:03`] 지금 팀원에게 준 내용과 다음번 고지 때 설명해야하는 것들 나열

## 1. 아래 내용 포함해서 고지하기 (멘토님 설명)
```
1. 알고리즘 결과 학습
 솔직히 말하면 이건 비추천이에요. Dijkstra는 정답을 보장하는데 그걸 흉내내는 AI는 정답 보장도 안 되면서 학습 비용만 드는 셈이거든요. 알고리즘 개선으로 푸는 걸 추천드립니다.
2. 두 모델 다 하면 범위가 너무 큰가
 네, 확실히 큽니다. 저같으면 경로 생성은 알고리즘으로 두고 추천만 AI로 갈거 같아요. 추천 쪽이 "정답이 없는 문제"라 AI를 쓸 명분도 확실하고, 포트폴리오에서 설명하기도 훨씬 좋아요.
3. 팀이 직접 라벨링할 때 주의할 점
 사람마다 좋은 경로 기준이 달라서 라벨이 흔들리는 게 제일 큰 함정이에요. 라벨링 전에 평가 기준표부터 문서로 합의하고, 점수 매기기보다는 둘 중 어느 경로가 나은가 비교 방식이 훨씬 일관성 있게 나옵니다.
4. 공간 데이터를 경로 단위 Feature로 가공
 이건 저도 직접 해본 영역은 아니라서 정답이라고 말씀드리긴 어렵습니다. 다만 데이터마다 좌표계가 다를 수 있으니 통일 작업이 먼저고, 경로마다 주변 시설을 매번 검색하면 느려질 테니 성능 쪽은 미리 고민해보시면 좋을 것 같습니다.
5. 경로 알고리즘·AI 모델·공간 데이터·외부 API를 하나로 연결할 때 충돌 지점과 병목
 충돌은 대부분 데이터 형식에서 나요. 알고리즘이 만든 경로, AI 모델 입력, 공간 데이터, TMAP이 요구하는 좌표가 전부 형식이 달라서 중간 변환에서 어긋나기 쉬우니, 컴포넌트 간 주고받을 형식을 처음에 딱 정해두는 게 중요합니다.
6. 분업 방식
 나누신 방향은 괜찮은데 데이터 파이프라인을 제일 먼저 끝내야 AI 파트가 놀지 않아요. 그리고 AI 담당이 모델만 붙잡고 있으면 통합할 때 고생하니까, 처음부터 더미 응답이라도 주는 API 형태로 만들어서 백엔드랑 계속 붙여보면서 가는 걸 추천합니다.
7. 뺄 것
 2번에서 말씀드린 대로 경로 생성 AI를 빼는 게 제일 효과가 클것으로 보입니다.
8. 실서비스 느낌 내는 요령
 현업에서 제일 신경 쓰는 건 잘 될 때가 아니라 안 될 때예요. 조건에 맞는 경로가 없을 때, 로딩이 길어질 때, API가 죽었을 때 등등 화면이 어떻게 보이는지, 또는 어떻게 처리하는지가 완성도를 결정합니다.

그리고 추가적으로 궁금한게 이번 프로젝트에서 사용자가 조건을 자연어로 입력한다고 하는데 어떠한 질의를 예상하는지 궁금합니다. 문서상에선 적당히 짧은코스, 오르막이 별로 없는 길 이런 질문을 예시로 든거 같은데 이를 벗어난 질문을 할시 어떻게 처리하려는지 궁금합니다. (편집됨)
```
## 2. 프론트
- [] 에러 시에 어떻게 사용자에게 보여줄지 정의하기
- [] 관리자 페이지 만들기 (이거 관리자 페이지는 웹으로 만드는건 어떤지? 어짜피 로직이나 페이지를 따로 만들어서 어려워지는 부분은 많이 없을거같은데.)

## 3. 백엔드
- [] 사용자 기본 프로필(설정값) 만들어주기
- 


## 2. 해야하는 구체적인 체크리스트 (27일까지)
```md
# RunStop 진행 체크리스트

## 1. 프론트

* [ ] **React Native 기본 구조 및 기술 검증**

  * 프로젝트 구조 / 기본 문법 파악
  * Android 실행 환경 구축
  * 필요한 라이브러리 후보 확인

* [ ] **지도 기능 PoC**

  * 지도 라이브러리 / TMAP 연동
  * 현재 위치 및 위치 권한 처리
  * 서버에서 받은 위경도 / LineString 경로 Drawing
  * 편의점 / 화장실 / 가로등 등 Point 표시
  * 시설 종류별 ON/OFF 토글

* [ ] **러닝 기능 PoC**

  * GPS 주기 수집
  * 러닝 시작 / 일시정지 / 종료
  * 타이머 및 현재 러닝 상태 표시

* [ ] **모바일 환경 검증**

  * 백그라운드 / 화면 OFF 상태 GPS
  * Android 위치 권한 및 절전 정책 확인
  * GPS 데이터 로컬 임시 저장
  * 네트워크 단절 후 재전송 가능 여부 확인

* [ ] **UI 구체화**

  * 로그인 / 비회원 화면
  * 코스 추천 / 코스 비교 / 코스 상세
  * 러닝 진행 화면
  * 기록 / 마이페이지 / 즐겨찾기 화면
  * 실제 구현 가능한 기능 기준으로 Figma 수정

## 2. 데이터 및 알고리즘

* [ ] **사용 데이터 최종 정리**

  * 사용할 CSV / SHP 데이터 결정
  * 데이터별 컬럼 / 좌표계 / 품질 확인
  * 추가 데이터셋 탐색

* [ ] **공간 데이터 전처리 규격 확정**

  * 좌표계 통일
  * Point / LineString 등 Geometry 규격 결정
  * 결측치 / 중복 / 이상값 처리 기준 정의
  * 데이터별 전처리 공통 구조 생성

* [ ] **Route Feature 정의**

  * 편의점 / 화장실 접근성
  * 가로등 밀도
  * 보행자도로 비율
  * 평균 / 최대 경사
  * 총 상승 고도
  * 기타 환경 Feature

* [ ] **경로 생성 알고리즘 구체화**

  * `RouteRequest` 입력 규격 확인
  * 후보 지점 / 구간 탐색 방법
  * 후보 경로 생성 방법
  * Dijkstra 등 탐색 알고리즘 실험
  * 경로 후보 점수화 방법 정의

* [ ] **알고리즘 인터페이스 완성**

  * `RouteRequest`
  * 후보 경로 생성
  * Route Feature 계산
  * 후보별 점수 계산
  * `RouteResponse` 반환

* [ ] **AI 적용 준비**

  * 학습에 사용할 Route Feature 확정
  * 학습 데이터 생성 방식 검토
  * 어떤 부분을 AI화할지 최종 결정

## 3. 서비스 아키텍처 / 회색지대 기술

* [ ] **전체 서비스 구조 확정**

  * React Native → Node.js → PostGIS / Python Worker 구조
  * 각 컴포넌트 책임 범위 결정
  * 서비스 전체 요청 흐름 정리

* [ ] **DB / GIS 구조 확정**

  * 서비스 ERD 구체화
  * PostgreSQL + PostGIS 구조 테스트
  * CSV / SHP 저장 및 조회 방법
  * 경로 LineString 저장
  * 경로 주변 시설 공간 Query 테스트
  * GPS 이동 경로 저장 구조 결정

* [ ] **TMAP / 지도 / 서버 연결 검증**

  * TMAP 보행자 경로 API 사용 범위 파악
  * 서버에서 사용할 경로 데이터 형태 결정
  * 서버 → 프론트 → 지도 Drawing까지 PoC
  * 사용자가 경로를 수정할 경우 데이터 처리 방법 검토

* [ ] **파트 간 인터페이스 확정**

  * Frontend ↔ Backend API
  * `RouteRequest / RouteResponse DTO`
  * 경로 / 시설 Point JSON 규격
  * Backend ↔ Python Worker 요청 / 응답 규격
  * 에러 / 요청 ID / 상태 처리 규칙

* [ ] **Node.js ↔ Python Worker 구조 검증**

  * 상주 Python Worker 호출 방식
  * 비동기 요청 / 응답
  * 요청 식별
  * 동시 요청 처리
  * Worker 장애 / 재시작 최소 검증

* [ ] **외부 서비스 및 회색지대 기술 검증**

  * Google / Kakao OAuth
  * TMAP
  * LLM API
  * React Native GPS / Android 제약사항
  * 외부 API timeout / 실패 처리

* [ ] **통합 기준 관리**

  * 프론트 ↔ 서버 연결 확인
  * 서버 ↔ DB 공간 데이터 연결 확인
  * 서버 ↔ Python 알고리즘 연결 확인
  * 각 팀의 DTO / 데이터 규격 불일치 조정
  * 작은 단위 통합 테스트 반복

* [ ] **최종 서비스 테스트 설계**

  * GPS 오차
  * 백그라운드 / 화면 OFF
  * 네트워크 단절
  * 외부 API 실패
  * 데이터가 부족한 지역
  * Python Worker 장애
  * 실제 야외 러닝 전체 플로우
```
# 개인적으로 멘토님에게 하고싶은 질문
- 역할을 [데이터 및 알고리즘], [서비스 아키텍처 및 기타 기술 테스트], [프론트 UI/UX] 로 나누었는데 아직 불만은 없는것 같지만 누군가는 AI 학습에서 소외될 수 있다고 생각하는데 괜찮을까요
- 