현재 모바일 expo go 깔아서 테스트 중입니다.

로그인은 admin 계정으로 들어가도 문제 없어서 admin 계정으로 들어갔습니다.

# `/home`
아래의 섹션 카드인 `러닝 목표`, `최근 러닝`, `즐겨찾기 코스`에 대한 상태 조회를 하지 않습니다. 백엔드는 구현되어있어서 넣으면 될 것 같습니다. (자세한 내용은 모두 docs에 존재합니다.)
- `GET /api/goals/current`
- `GET /api/running-sessions`
- `GET /api/bookmarks/points`

