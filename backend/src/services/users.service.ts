/**
 * 프로필, 목표, 러닝, 즐겨찾기, 페이스 데이터를 조합해 현재 사용자의 마이페이지 요약을 만듭니다.
 */
export async function getMyPageSummary() {
}

/**
 * 현재 사용자와 프로필의 수정 가능한 필드를 변경합니다.
 */
export async function updateCurrentUser() {
}

/**
 * 외래 키로 연결된 기록을 보존하면서 현재 사용자를 탈퇴 상태로 변경합니다.
 */
export async function withdrawCurrentUser() {
}
