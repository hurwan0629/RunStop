/**
 * Python routing-worker의 상태 확인 엔드포인트를 호출합니다.
 */
export async function checkRoutingWorkerHealth() {
}

/**
 * Python routing-worker에 경로 추천 요청을 보냅니다.
 */
export async function requestRouteRecommendations() {
}
